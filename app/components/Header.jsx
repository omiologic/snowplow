import React from 'react';
import Navbar from '../ui/Navbar';

export default class HeaderComponent extends React.Component {
  render() {
    return (
      <Navbar>
        <div>Logo</div>
        <div>
            this is menu place holder
        </div>
      </Navbar>
    );
  }
}
